apply plugin: 'org.springframework.boot'

dependencies {
    implementation(project(":account-book-domain"))

    implementation 'org.apache.kafka:kafka-clients:2.8.0'
    implementation 'com.google.code.gson:gson:2.10'
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}